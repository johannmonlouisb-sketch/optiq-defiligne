{
  "name": "optiq-tech",
  "version": "1.0.0",
  "description": "OptiQ Technicien — Application mobile DEA",
  "scripts": {
    "build": "echo 'No build step — static HTML'",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "apk": "npx cap sync android && cd android && .\\gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
